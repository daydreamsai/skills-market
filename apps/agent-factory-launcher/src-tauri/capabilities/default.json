{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the launcher window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
